# Home components

John Remy: the home sections. The hero composes the first impression; FeaturedWork
brings the work forward. The 3D scene (DESIGN.md §7) lives in `HeroScene.tsx` — a
metallic primary-blue form on a soft neutral studio, loaded client-only via
`next/dynamic` and skipped entirely for reduced-motion visitors.
